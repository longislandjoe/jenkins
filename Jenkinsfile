pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                sh 'ls' 
            }
        }
        stage('Test'){
            steps {
                sh 'ls -lart'
                
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
